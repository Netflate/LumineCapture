// Panel behaviour: what each panel shows, where it sits and where its edits land.
// The panels themselves (state + drawing) live in ui/.

pub mod color;
pub mod model;
pub mod settings;
pub mod toolbar;
